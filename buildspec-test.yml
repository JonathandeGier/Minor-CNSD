version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Tests started on `date`
      - mvn test
  post_build:
    commands:
      - echo Tests completed on `date`
